<odoo>
    <data>
<!--
        <template id="listing">
          <ul>
            <li t-foreach="objects" t-as="object">
              <a t-attf-href="#{ root }/objects/#{ object.id }">
                <t t-esc="object.display_name"/>
              </a>
            </li>
          </ul>
        </template>
        <template id="object">
          <h1><t t-esc="object.display_name"/></h1>
          <dl>
            <t t-foreach="object._fields" t-as="field">
              <dt><t t-esc="field"/></dt>
              <dd><t t-esc="object[field]"/></dd>
            </t>
          </dl>
        </template>

        <record id="credit_limit_change_email_template" model="mail.template">
          <field name="name">Credit Limit changed</field>
          <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
          <field name="subject">Change of Credit Limit</field>
          <field name="email_to">davidperez1966@yahoo.com.mx</field>
          <field name="lang">${object.lang}</field>
          <field name="model_id" ref="base.model_res_partner"/>
          <field name="auto_delete" eval="True"/>
          <field name="body_html">
            <![CDATA[
              <p>Dear Administrator,<br/><br/>
	            This message is a warnging to let you know that the Credit Limit assigned to<br/>
              ${object.name} has been changed by ${user.name}
              </p>
              
              Regards,<br/>
              ${(object.company_id.name)}  
            ]]>
          </field>
        </record-->
    </data>
</odoo>